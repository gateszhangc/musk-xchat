apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bloodhounds-homes-tls
spec:
  secretName: bloodhounds-homes-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - bloodhounds.homes
    - www.bloodhounds.homes
